# Devin API (from app.devin.ai → Settings → API Keys)
DEVIN_API_KEY=
# Personal GitHub token for the automation (fine-grained PAT on Soham4001A;
# scopes: repo contents R/W, issues R/W, pull requests R/W, webhooks) — later
GH_PERSONAL_TOKEN=
